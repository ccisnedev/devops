# deploy.yaml — Configuración de despliegue para Flutter Web
# Generado por: Publish-FlutterWeb -Init
# name y version se leen siempre de pubspec.yaml
# NO incluya secretos en este archivo — este archivo SÍ se versiona.

# ─── Servidor ─────────────────────────────────────────
server: app-server            # alias en ~/.ssh/config

# ─── Nginx ────────────────────────────────────────────
port: 4000                    # puerto donde nginx escuchará para esta app
